name: python V2rayCollector Script

on:
  schedule:
   # Run every 55 minutes
    - cron: '*/55 * * * *'
  workflow_dispatch:

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'

      - name: Configure Git
        run: |
          git config --global user.email "darknessm.427@gmail.com"
          git config --global user.name "darknessm427"
          git remote set-url origin https://github.com/darknessm427/V2ray-Config-Location.git
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests
          python -m pip install beautifulsoup4

      - name: Run Python script
        run: |
          python main.py  # Replace with your script's path
          git add -A
          git commit -m "👾🤖Auto update💀 ✔ - $(TZ='Asia/Tehran' date '+%Y-%m-%d %H:%M:%S')"
          git pull --rebase origin main
          git push
